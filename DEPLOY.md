# 🚀 배포 가이드 — 이대로 따라하면 판매 시작

> 코드는 완성되어 있습니다. 아래는 **사장님(당신)만 할 수 있는 일**입니다.
> 전부 무료로 시작할 수 있고, 30분이면 끝납니다.

## 1단계. Vercel 배포 (무료, 5분)

1. https://vercel.com 가입 (GitHub 계정으로)
2. 이 폴더를 GitHub에 올리기:
   ```bash
   # 클로드에게 "깃허브에 올려줘"라고 하면 해드립니다 (gh 로그인 필요)
   ```
3. Vercel에서 "Add New Project" → 방금 올린 저장소 선택 → Deploy
4. 끝. `https://프로젝트명.vercel.app` 주소가 생깁니다.

## 2단계. 토스페이먼츠 가입 (돈 받는 통로, 무료)

1. https://pay.toss.im 에서 가맹점 가입
   - **사업자등록 필요** — 없다면 홈택스에서 통신판매업 간이과세자로 등록 (무료, 온라인 30분)
   - 통신판매업 신고도 필요 (구청 온라인, 등록면허세 약 4만원/년)
2. 가입 후 [개발자센터](https://developers.tosspayments.com) → API 키 확인
3. 키 2개를 꽂습니다:
   - **클라이언트 키**: `order/order.js` 맨 위 `TOSS_CLIENT_KEY` 값 교체
   - **시크릿 키**: Vercel 프로젝트 → Settings → Environment Variables →
     `TOSS_SECRET_KEY` = `live_gsk_...` 추가
4. 정산 계좌(= 돈 들어올 통장) 등록은 토스 가맹점 어드민에서.

> 사업자등록 전에도 **테스트 키**로 전 과정을 미리 볼 수 있습니다.
> 지금 코드에는 공개 테스트 키가 들어 있어 결제창까지 이미 동작합니다.

## 3단계. 주문 알림 받기 (선택, 5분)

디스코드 앱 설치 → 아무 서버 만들기 → 채널 설정 → 연동 → 웹훅 만들기 → URL 복사
→ Vercel 환경변수 `ORDER_WEBHOOK_URL` 에 붙여넣기.
이제 주문이 들어오면 휴대폰으로 바로 알림이 옵니다.

## 4단계. 주문 처리 (건당 10분)

주문 알림이 오면:
```bash
# 1. 주문 내용을 JSON 파일로 저장 (알림 내용 복사 → 클로드에게 "이 주문 처리해줘")
node scripts/make-invite.mjs 주문.json 신랑-신부
# 2. 고객이 보낸 사진을 i/신랑-신부/img/ 에 넣고 data.js 경로 수정
# 3. git push (Vercel 자동 배포)
# 4. 고객에게 링크 전송: https://도메인/i/신랑-신부/
```
**이 전 과정을 클로드에게 시키면 됩니다.** "주문 왔어, 처리해줘" 한 마디면 충분.

## 5단계. 손님 모으기 (매출의 전부)

만들어두면 팔리지 않습니다. 채널 우선순위:
1. **네이버 카페** — 결혼준비 카페(다이렉트 웨딩 등)에서 후기·정보성 글
2. **인스타그램** — 템플릿 미리보기 릴스 (샘플 링크 프로필에)
3. **크몽/아이디어스** — 수수료 있지만 초기 트래픽 확보에 최고
4. **네이버 스마트스토어** — "모바일청첩장" 검색 수요 흡수
5. 지인 결혼식 1건 무료 제작 → 후기 확보

## 매출 계산

| 월 판매 | 매출 | 비용(서버) | 순익 |
|--------|------|-----------|------|
| 50건 | 124만원 | 0원 | ~120만원 |
| 200건 | 498만원 | 0원 | ~480만원 |
| 400건 | 996만원 | 0원 | **~960만원** |

토스 결제 수수료(약 3%)와 세금 별도. 서버비는 Vercel 무료 티어로 충분.
